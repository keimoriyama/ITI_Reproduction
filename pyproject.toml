[project]
name = "iti-reproduction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "accelerate>=1.10.0",
    "datasets>=4.0.0",
    "numpy>=2.3.2",
    "pydantic>=2.11.7",
    "pydantic-settings>=2.10.1",
    "torch>=2.8.0",
    "transformers>=4.55.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/iti_reproduction"]

[tool.hatch.version]
path = "src/iti_reproduction/__init__.py"

[dependency-groups]
dev = [
    "ipdb>=0.13.13",
    "pytest>=8.4.1",
]
